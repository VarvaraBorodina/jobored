import styled from 'styled-components'

const Container = styled.div
`
cursor: pointer;
`

const textStyles = {
    borderRadius: '8px',
    fontSize: '14px', 
    paddingLeft: '12px', 
    fontWeight: 400,
    cursor: 'pointer',
}

export {Container, textStyles}